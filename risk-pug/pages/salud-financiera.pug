extends ../layouts/page-panel-head.pug

block vars
  - title = 'Salud Financiera';
  - titleVisible = title;
  - titleBC = title;
  - panelActive = [title];
  - 
    breadcrumbLinks = [
      ['#', 'Inicio'],
      ['#', title]
    ];